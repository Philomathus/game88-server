<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tv.game88.game.api.mapper.GameTypeWithMapper">
    <insert id="insertBatch">
        insert into game_type_with (type_id, game_info_id, sort) VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.typeId}, #{item.gameInfoId}, #{item.sort})
        </foreach>
    </insert>

    <select id="selectTypeIdByPlatformId" resultType="java.lang.Long">
        select distinct type_id
        from game_type_with
        where game_info_id in (select id from game_info where platform_id = #{platformId})
    </select>

    <select id="selectGameTypeWithList" resultType="tv.game88.game.api.entity.GameTypeWith">
        select w.type_id typeId, w.game_info_id gameInfoId, w.sort, i.name gameInfoName, i.icon gameInfoIcon
        from game_type_with w
                 left join game_info i on i.id = w.game_info_id
        where w.type_id = #{typeId}
        order by w.sort
    </select>
</mapper>