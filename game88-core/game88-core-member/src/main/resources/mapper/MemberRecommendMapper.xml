<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="tv.game88.core.member.mapper.MemberRecommendMapper">
    <select id="getRecommendDetailList" resultType="tv.game88.core.member.dto.RspMemberRecommend">
        select commission, create_time, code, level
        from member_recommend
        where inviter_id = #{memberId}
        <if test="code != null and code != ''">
            and code = #{code}
        </if>
        order by create_time desc
    </select>

    <select id="getMyRecommend" resultType="tv.game88.core.member.dto.RspMyRecommend">
        select *
        from (SELECT invite_money historySion, inviter_code FROM member_info WHERE id = #{inviterId}) t1,
             (SELECT ifnull(SUM(commission), 0) todaySion FROM `member_recommend` WHERE inviter_id = #{inviterId} and DATEDIFF(create_time,NOW()) = 0) t2,
             (SELECT ifnull(SUM(commission), 0) yesterdaySion FROM `member_recommend` WHERE inviter_id = #{inviterId} and DATEDIFF(create_time,NOW()) = -1) t3,
             (SELECT ifnull(SUM(commission), 0) canSion FROM `member_recommend` WHERE inviter_id = #{inviterId} and status = 0) t4
    </select>

    <update id="updateCommissionByBatch">
        UPDATE member_recommend
        SET status = 1
        WHERE id IN
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
        and status = 0
    </update>
</mapper>