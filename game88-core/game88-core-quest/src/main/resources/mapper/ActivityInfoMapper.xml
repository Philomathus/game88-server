<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tv.game88.core.quest.mapper.ActivityInfoMapper">
    <resultMap type="tv.game88.core.quest.entity.ActivityInfo" id="ActivityInfoResult">
        <result property="id" column="id" />
        <result property="icon" column="icon" />
        <result property="title" column="title" />
        <result property="typeId" column="type_id" />
        <result property="content" column="content" />
        <result property="effect" column="effect" />
        <result property="type" column="type" />
        <result property="url" column="url" />
        <result property="sort" column="sort" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectActivityInfoVo">
        select id,
               icon,
               title,
               type_id,
               content,
               effect,
               type,
               url,
               sort,
               create_by,
               create_time,
               update_by,
               update_time
        from activity_info
    </sql>

    <select id="selectActivityInfoList" parameterType="tv.game88.core.quest.entity.ActivityInfo"
            resultMap="ActivityInfoResult">
        <include refid="selectActivityInfoVo" />
        <where>
            <if test="icon != null  and icon != ''">
                and icon = #{icon}
            </if>
            <if test="title != null  and title != ''">
                and title = #{title}
            </if>
            <if test="typeId != null">
                and type_id = #{typeId}
            </if>
            <if test="content != null  and content != ''">
                and content = #{content}
            </if>
            <if test="effect != null">
                and effect = #{effect}
            </if>
            <if test="type != null">
                and type = #{type}
            </if>
            <if test="url != null  and url != ''">
                and url = #{url}
            </if>
            <if test="sort != null">
                and sort = #{sort}
            </if>
            <if test="createBy != null  and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateBy != null  and updateBy != ''">
                and update_by = #{updateBy}
            </if>

<!--            <if test="params.beginTime != null and params.beginTime != ''">-->
<!--                AND update_time <![CDATA[ >= ]]> STR_TO_DATE(#{params.beginTime}, '%Y-%m-%d %H:%i:%s')-->
<!--            </if>-->
<!--            <if test="params.endTime != null and params.endTime != ''">-->
<!--                AND update_time <![CDATA[ <= ]]> STR_TO_DATE(#{params.endTime}, '%Y-%m-%d %H:%i:%s')-->
<!--            </if>-->

        </where>
        order by sort asc
    </select>
</mapper>