<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tv.game88.platform.api.mapper.ReportPlamGameschildsMapper">
    <resultMap type="tv.game88.platform.api.entity.ReportPlamGameschilds" id="ReportPlamGameschildsResult">
        <result property="gameUuid" column="game_uuid" />
        <result property="gameagent" column="gameagent" />
        <result property="gameplame" column="gameplame" />
        <result property="agentchild" column="agentchild" />
        <result property="agentchildname" column="agentchildname" />
        <result property="gamepepole" column="gamepepole" />
        <result property="gametouzhu" column="gametouzhu" />
        <result property="gamecell" column="gamecell" />
        <result property="gamebet" column="gamebet" />
        <result property="gamerevenve" column="gamerevenve" />
        <result property="gameprofit" column="gameprofit" />
        <result property="plampfit" column="plampfit" />
        <result property="bili" column="bili" />
        <result property="begindate" column="begindate" />
    </resultMap>

    <sql id="selectReportPlamGameschildsVo">
        select game_uuid,
        gameagent,
        gameplame,
        agentchild,
        agentchildname,
        gamepepole,
        gametouzhu,
        gamecell,
        gamebet,
        gamerevenve,
        gameprofit,
        plampfit,
        bili,
        <choose>
            <when test="endDate != null">
                CONCAT( #{begindate}, ' - ', #{endDate} ) as begindate
            </when>
            <otherwise>
                begindate
            </otherwise>
        </choose>
        from report_plam_gameschilds
    </sql>

    <select id="selectReportPlamGameschildsList" parameterType="tv.game88.platform.api.entity.ReportPlamGameschilds"
            resultMap="ReportPlamGameschildsResult">
        <include refid="selectReportPlamGameschildsVo" />
        <where>
            <if test="gameagent != null  and gameagent != ''">
                and gameagent = #{gameagent}
            </if>
            <if test="gameplame != null  and gameplame != ''">
                and gameplame = #{gameplame}
            </if>
            <if test="agentchild != null  and agentchild != ''">
                and agentchild = #{agentchild}
            </if>
            <if test="agentchildname != null  and agentchildname != ''">
                and agentchildname like concat('%', #{agentchildname}, '%')
            </if>
            <if test="gamepepole != null">
                and gamepepole = #{gamepepole}
            </if>
            <if test="gametouzhu != null">
                and gametouzhu = #{gametouzhu}
            </if>
            <if test="gamecell != null">
                and gamecell = #{gamecell}
            </if>
            <if test="gamebet != null">
                and gamebet = #{gamebet}
            </if>
            <if test="gamerevenve != null">
                and gamerevenve = #{gamerevenve}
            </if>
            <if test="gameprofit != null">
                and gameprofit = #{gameprofit}
            </if>
            <if test="plampfit != null">
                and plampfit = #{plampfit}
            </if>
            <if test="bili != null  and bili != ''">
                and bili = #{bili}
            </if>
            <if test="begindate != null and begindate != ''">
                and begindate <![CDATA[ >= ]]> #{begindate}
            </if>
            <if test="endDate != null and endDate != ''">
                and begindate <![CDATA[ <= ]]> #{endDate}
            </if>
        </where>
    </select>

    <select id="getPlatformIdByGameUuid" resultType="java.lang.String">

        select
            gameplamid
        from
            report_plam_gameschilds
        where
            game_uuid = #{gameUuid}

    </select>
    <select id="selectByBettorsCounts" resultType="tv.game88.platform.api.entity.ReportPlamGameschilds">
        select * from (
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild'from member_game_data0 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate} and platform_id = #{gameplamid} and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell', ROUND(sum(profit),2) as 'gameprofit', account as 'agentchild' from member_game_data1 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate} and platform_id = #{gameplamid} and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data2 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data3 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data4 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell', ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data5 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data6 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data7 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data8 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account union all
                          select ROUND(sum(cell_score),2) as 'gamecell',ROUND(sum(profit),2) as 'gameprofit',account as 'agentchild' from member_game_data9 where  game_end_time <![CDATA[ >= ]]> #{begindate} and game_end_time <![CDATA[ <= ]]> #{endDate}  and platform_id = #{gameplamid}  and agent = #{gameagent} and kind_id = #{agentchild}  GROUP BY account ) d
    </select>
</mapper>
